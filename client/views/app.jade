!!! 5
html(lang="en")
  head
    != SocketStream
    meta(charset="utf-8")
    title Regex2Strings
  body
    #content
      a(href='https://github.com/socketstream/socketstream')
        img(src='/images/logo.png', alt='SocketStream Logo', width=160, height=160)
      h1 Regex2Strings: Type a regex to see all strings that would match it

      // QUICK CHAT DEMO
      form#demo(onsubmit='return false')
        h3 Type your regex here
        input(id='regex', type='text')
        h3 And strings will be shown here
        textarea(id='strings')
